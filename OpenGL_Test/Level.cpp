#include "Level.h"

#include <iostream>
#include <fstream>
#include <string>

using namespace Levels;

std::string Level::ReadFile(std::string path) {
	std::string ans = "";
	std::fstream file;

	file.open(path);
	return ans;
}